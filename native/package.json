{
  "name": "empty-project-template",
  "main": "node_modules/expo/AppEntry.js",
  "private": true,
  "scripts": {
    "watch": "cpx \"../shared/**/*.*\" ./shared/ -w",
    "start": "concurrently \"npm run watch\" \"expo start\"",
    "android": "concurrently \"npm run watch\" \"expo start --android\"",
    "ios": "concurrently \"npm run watch\" \"expo start --ios\"",
    "eject": "expo eject"
  },
  "dependencies": {
    "expo": "^29.0.0",
    "react": "16.3.1",
    "react-native": "https://github.com/expo/react-native/archive/sdk-29.0.0.tar.gz"
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "cpx": "^1.5.0"
  }
}
